============
Installation
============

At the command line either via easy_install or pip::

    $ easy_install negative-inline-editor
    $ pip install negative-inline-editor

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv negative-inline-editor
    $ pip install negative-inline-editor
